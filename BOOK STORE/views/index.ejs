<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Books</title>
</head>
<style>

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f7f9fb;
            color: #333;
        }
        .navbar {
            background-color: #ffffff;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 40px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            position: relative;
            animation: slideIn 1s ease-out;
        }

        .logo {
            font-size: 24px;
            font-weight: bold;
            color: #5a9baf;
            transition: color 0.3s;
        }

        .logo:hover {
            color: #4a8a9b;
        }

        .nav-links {
            list-style: none;
            display: flex;
            gap: 30px;
        }

        .nav-links li {
            position: relative;
        }

        .nav-links a {
            text-decoration: none;
            color: #555;
            font-size: 18px;
            font-weight: 600;
            transition: color 0.3s ease, transform 0.3s ease;
        }
        .nav-links a:hover {
            color: #5a9baf;
            transform: translateY(-3px);
        }

        .nav-links li::after {
            content: '';
            position: absolute;
            width: 100%;
            height: 2px;
            background-color: #5a9baf;
            bottom: -5px;
            left: 0;
            transform: scaleX(0);
            transform-origin: bottom right;
            transition: transform 0.3s ease-out;
        }

        .nav-links li:hover::after {
            transform: scaleX(1);
            transform-origin: bottom left;
        }
        @keyframes slideIn {
            from {
                transform: translateY(-30px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }



        body {
            font-family: 'Arial', sans-serif;
            background-color: #f7f9fb;
            color: #333;
        }

        h1 {
            text-align: center;
            margin-bottom: 30px;
            color: #5a9baf;
            animation: fadeIn 2s ease-out;
        }

        form {
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 600px;
            margin: 0 auto 40px auto;
            animation: slideIn 1s ease-out;
        }

        form input, form button {
            width: 100%;
            max-width: 575px;
            padding: 12px;
            margin: 10px 0;
            border-radius: 8px;
            border: 1px solid #ccc;
            font-size: 16px;
            outline: none;
            transition: border-color 0.3s;
        }

        form input:focus, form button:hover {
            border-color: #5a9baf;
        }

        form button {
            background-color: #5a9baf;
            color: white;
            cursor: pointer;
            font-size: 18px;
            transition: background-color 0.3s ease;
        }

        form button:hover {
            background-color: #4a8a9b;
        }

        .card-container {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 20px;
            margin-top: 20px;
            animation: fadeIn 3s ease-out;
        }

        .card {
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .card:hover {
            transform: scale(1.05);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }

        .card-image img {
            width: 100%;
            height: 300px;
        }

        .card-content {
            padding: 15px;
            background-color: #f9f9f9;
        }

        .card-content h3 {
            color: #5a9baf;
            margin-bottom: 10px;
        }

        .card-content p {
            color: #555;
            margin: 5px 0;
        }

        .card-actions {
            background-color: #eee;
            padding: 10px;
            text-align: center;
            border-top: 1px solid #ddd;
        }

        .edit-button, .delete-button {
            padding: 8px 16px;
            margin: 5px;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
        }

        .edit-button {
            background-color: #5a9baf;
            color: white;
        }

        .delete-button {
            background-color: #e57373;
            color: white;
        }

        .edit-button:hover {
            background-color: #4a8a9b;
        }

        .delete-button:hover {
            background-color: #d32f2f;
        }
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        @keyframes slideIn {
            from {
                transform: translateY(-20px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }



</style>
<body>

    <nav class="navbar">
        <div class="logo">BOOK STORE</div>
        <ul class="nav-links">
          <li><a href="">Home</a></li>
          <li><a href="">About</a></li>
          <li><a href="">Categories</a></li>
          <li><a href="">Blog</a></li>
          <li><a href="">Contact</a></li>
        </ul>
    </nav>
    <h1>Welcome</h1>
    <form action="/adddata" method="post">
      <input type="url" placeholder="Enter Image URL" name="image" required>
        <input type="text" placeholder="Enter Book Name" name="name" required>
        <input type="text" placeholder="Enter Author Name" name="Author" required>
        <input type="text" placeholder="Enter Price" name="Price" required>
        <input type="date" placeholder="Enter Published Date" name="Date" required>
        <button type="submit">Add Data</button>
    </form>
    <% if (book.length === 0) { %>
      <p>No book found!</p>
  <% } else { %>
      <div class="card-container">
          <% book.map((e, i) => { %>
              <div class="card">
                  <div class="card-image">
                      <img src="<%= e.image %>" alt="Book Image">
                  </div>
                  <div class="card-content">
                      <h3><%= e.name %></h3>
                      <p><strong>ID:</strong> <%= e.id %></p>
                      <p><strong>Author:</strong> <%= e.Author %></p>
                      <p><strong>Price:</strong> <%= e.Price %></p>
                      <p><strong>Published Date:</strong> <%= e.Date %></p>
                  </div>
                  <div class="card-actions">
                      <a href="/editdata?id=<%= e.id %>" class="edit-button">Edit Data</a>
                      <a href="/deletedata?id=<%= e.id %>" class="delete-button">Delete Data</a>
                  </div>
              </div>
          <% }); %>
      </div>
  <% } %>
  
</body>

</html>