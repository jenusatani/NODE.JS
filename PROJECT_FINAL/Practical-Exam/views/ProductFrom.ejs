<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Form</title>
    <link href="https://fonts.googleapis.com/css?family=Poppins:200i,400&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(to right, #7FFF3F, #34840B);
        }

        #wrapper {
            width: 90%;
            max-width: 400px;
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(10px);
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.3);
            color: white;
            text-align: center;
        }

        h1 {
            font-size: 24px;
            margin-bottom: 15px;
            color: #fff;
        }

        .form-set {
            text-align: left;
            margin-bottom: 15px;
        }

        label {
            font-size: 14px;
            display: block;
            margin-bottom: 5px;
            color: #fff;
        }

        input[type="text"] {
            width: 100%;
            padding: 10px;
            border-radius: 8px;
            border: none;
            outline: none;
            font-size: 16px;
            background: rgba(255, 255, 255, 0.2);
            color: white;
            transition: 0.3s ease-in-out;
        }

        input::placeholder {
            color: rgba(255, 255, 255, 0.7);
        }

        input:focus {
            background: rgba(255, 255, 255, 0.3);
            border: 1px solid #7FFF3F;
        }

        .submit-button {
            width: 100%;
            padding: 12px;
            border: none;
            border-radius: 8px;
            background: #7FFF3F;
            color: #34840B;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            transition: 0.3s;
            margin-top: 15px;
        }

        .submit-button:hover {
            background: #66D336;
            color: white;
        }

    </style>
</head>
<body>

    <div id="wrapper">
        <div class="container">
            <header class="header">
                <div class="title-header">
                    <h1 id="title" class="text-center">Add Product</h1>
                </div>
            </header>    

            <form id="survey-form" action="/insert" method="post">
                <div class="form-set">
                    <label id="name-label" for="name">Product Name</label>
                    <input type="text" name="name" id="name" class="form-control" placeholder="Product Name" required>
                </div>

                <div class="form-set">
                    <label id="email-label" for="email">Price</label>
                    <input type="text" name="email" id="email" class="form-control" placeholder="Price" required>
                </div>

                <div class="form-set">
                    <button type="submit" id="submit" class="submit-button">Submit</button>
                </div>
            </form>
        </div>
    </div>

</body>
</html>
