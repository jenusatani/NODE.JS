<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Page</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background: linear-gradient(to right, #667eea, #764ba2);
            color: white;
            text-align: center;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(10px);
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0px 5px 20px rgba(0, 0, 0, 0.2);
            width: 350px;
            text-align: center;
        }

        h1 {
            font-size: 24px;
            margin-bottom: 20px;
        }

        form {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        input[type="text"], input[type="radio"], input[type="checkbox"] {
            margin-top: 5px;
            padding: 8px;
            width: 100%;
            border-radius: 8px;
            border: none;
            outline: none;
            font-size: 16px;
            background: rgba(255, 255, 255, 0.2);
            color: white;
        }

        input::placeholder {
            color: rgba(255, 255, 255, 0.7);
        }

        label {
            font-size: 14px;
            margin-top: 5px;
            display: block;
        }

        .radio-group, .checkbox-group {
            display: flex;
            justify-content: center;
            gap: 10px;
        }

        button {
            width: 100%;
            padding: 12px;
            border: none;
            border-radius: 8px;
            background: #ff7eb3;
            color: white;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            transition: 0.3s;
            margin-top: 15px;
        }

        button:hover {
            background: #ff3f80;
        }

    </style>
</head>
<body>

    <div class="container">
        <h1>Edit Your Data</h1>
        <form action="/update" method="post">
            <input type="hidden" name="id" value="<%= user.id %>">
            <input type="text" placeholder="Enter your name" name="name" value="<%= user.name %>">
            <input type="text" placeholder="Enter your email" name="email" value="<%= user.email %>">

            <label>Gender</label>
            <div class="radio-group">
                <label><input type="radio" name="gender" value="man" <%= user.gender === 'man' ? 'checked' : '' %>> Man</label>
                <label><input type="radio" name="gender" value="woman" <%= user.gender === 'woman' ? 'checked' : '' %>> Woman</label>
                <label><input type="radio" name="gender" value="others" <%= user.gender === 'others' ? 'checked' : '' %>> Others</label>
            </div>

            <label>Recipe Preference</label>
            <div class="checkbox-group">
                <label><input type="checkbox" name="prefer" value="Gujarati-recipe" <%= user.prefer.includes('Gujarati-recipe') ? 'checked' : '' %>> Gujarati</label>
                <label><input type="checkbox" name="prefer" value="Punjabi-recipe" <%= user.prefer.includes('Punjabi-recipe') ? 'checked' : '' %>> Punjabi</label>
                <label><input type="checkbox" name="prefer" value="chinese-recipe" <%= user.prefer.includes('chinese-recipe') ? 'checked' : '' %>> Chinese</label>
            </div>

            <button type="submit">UPDATE</button>
        </form>
    </div>

</body>
</html>
